---
// Galerie de l'accueil : les 9 premières photos + flèche vers la page complète.
import { galleryTiles } from '../../data/gallery';

const tiles = galleryTiles.slice(0, 9);
---
<section class="gal pad" id="galerie">
  <div class="wrap">
    <div class="ghead reveal">
      <div>
        <div class="eyebrow" style="margin-bottom:16px">Galerie</div>
        <div class="big">Un avant-goût du <em>terrain</em></div>
      </div>
      <a href="/galerie" class="more" aria-label="Voir toute la galerie">
        <span>Voir tout</span>
        <svg viewBox="0 0 32 32" width="46" height="46" aria-hidden="true">
          <circle cx="16" cy="16" r="15" fill="none" stroke="currentColor" stroke-width="1.2"/>
          <path d="M13 10 L20 16 L13 22 Z" fill="currentColor"/>
        </svg>
      </a>
    </div>
    <div class="ggrid reveal">
      {tiles.map((t, i) => (
        <a class="gtile" href={t.src} aria-label={t.alt}>
          <picture>
            <source
              type="image/webp"
              srcset={`${t.base}-800.webp 800w, ${t.base}-1400.webp 1400w`}
              sizes="(max-width: 640px) 100vw, (max-width: 960px) 50vw, 33vw"
            />
            <img
              src={t.src}
              alt={t.alt}
              width="2000"
              height="1333"
              loading={i < 3 ? 'eager' : 'lazy'}
              decoding="async"
            />
          </picture>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .ghead {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 24px;
    margin-bottom: 40px;
    flex-wrap: wrap;
  }
  .more {
    display: inline-flex;
    align-items: center;
    gap: 14px;
    color: var(--ink);
    font-family: var(--sans);
    text-transform: uppercase;
    letter-spacing: 2px;
    font-size: 12px;
    font-weight: 700;
    transition: color .3s ease;
  }
  .more:hover { color: var(--sand); }
  .more svg { transition: transform .3s ease; }
  .more:hover svg { transform: translateX(4px); }

  .ggrid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 14px;
    grid-auto-rows: initial;
  }
  .gtile {
    position: relative;
    overflow: hidden;
    border-radius: 2px;
    display: block;
    aspect-ratio: 3 / 2;
    background: var(--cream);
  }
  .gtile picture,
  .gtile picture img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform .8s cubic-bezier(.5, 0, .2, 1);
  }
  .gtile:hover picture img { transform: scale(1.05); }

  @media (max-width: 960px) {
    .ggrid { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .ggrid { grid-template-columns: 1fr; }
  }
</style>
